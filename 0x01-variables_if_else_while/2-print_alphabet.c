#include <stdio.h>
/**
 * main - This is our main entry for the function
 * Return: Zero to signify code success
 */

int main(void)
{
	char b;

	b = 'a';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
